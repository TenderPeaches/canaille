<%# add created row to the list %>
<%= turbo_stream.append "user-service-provider-accesses" do %>
    <%# is_admin = true because it must be true to have gotten to this point %>
    <%= render "user_service_provider_accesses/user_service_provider_access", can_edit: true, user_service_provider_access: @user_service_provider_access %>
<% end %>
<%# clear the form %>
<%# todo have an option to create more so the form stays %>
<%= turbo_stream.update "user-service-provider-access-form" %>
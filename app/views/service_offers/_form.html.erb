<%= form_with model: service_offer, do |form| %>
    <%= render partial: 'service_offers/fields', locals: { form: form } %>
    <%= form.submit service_offer.new_record? ? t('models.create') : t('models.update') %>
<% end %>
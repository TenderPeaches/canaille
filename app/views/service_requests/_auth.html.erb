<%= tag.section id: 'log-in' do %>
    <%= tag.h2 t('models.session.create_title') %>
    <%= render "devise/sessions/form", resource: User.new, resource_name: :user, embedded: true, url: new_service_request_path, id: service_request.id %>
<% end %>

<%= tag.section id: 'sign-up' do %> 
    <%= tag.h2 t('models.user.create_title') %>
    <%= render "users/form", user: User.new, embedded: true, is_client: true %>
<% end %>
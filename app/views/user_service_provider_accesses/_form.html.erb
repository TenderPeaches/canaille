<%= form_with model: user_service_provider_access do |form| %>
    <%= render partial: "user_service_provider_access/fields", locals: { form: form } %>
    <%= form.submit must_create?(user_service_provider_access) ? t('models.create') : t('models.update') %>
<% end %>
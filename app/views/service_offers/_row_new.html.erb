<%= tag.tr do %>
    <%# colspan: as many as there are columns in _row.html.erb %>
    <%= tag.td colspan: 4 do %>
        <%= link_to t('models.service_offer.create_title'), new_service_provider_service_offer_path(service_provider), class: "button", data: { turbo_stream: true } %>
    <% end %>
<% end %>